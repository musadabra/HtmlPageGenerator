/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package codeGenerator.mainContent;

/**
 *
 * @author HP PRO
 */
public class Element {
   /*protected QueueAsLinkedList code;
    
    public Element(QueueAsLinkedList code)
    {
        this.code = code;
    }
    
    public void insertElement(String idOfDiv)
    {
        Iterator it = code.iterator();
        while(it.hasNext())
        {
            Object obj = it.next();
            String line = (String) obj;
            String regex = "\t";
            String[] linesplit = line.split(regex);
            
            //CHECKING THE SPLITED WORDS TO MAKE SURE THE MAKE A MEANIGN 
            //SUCH AS <div, id="id"
            for (int i = 0; i  < linesplit.length ; i++) {
                // IF THE CHARACTER IS THESAME AS THE LISTED JOIN IT WITH THE NEXT ONE TO MAKE IT FULL
                while(linesplit[i].matches("[<]||[id]||[id=]")){
                    linesplit[i] += linesplit[i+1];
                }
                
            }
            
            //COMPARING THE SPLITTED WORDS TO FIND THE ID OF THE LINE OF CODE
            for (int i = 0; i < linesplit.length; i++) {
                if(linesplit[i].toLowerCase().matches(""));
            }
            
            
        }
    }
    
    public void addClassToElement(String idOfDiv)
    {
        
    }
    
    public void removeClassFromElement()
    {
        
    }
    
    public void moveElement()
    {
    
    }*/  
}
